import { ComingSoon } from "@/components/coming-soon";

export default function RealtimePage() {
  return (
    <ComingSoon
      title="Realtime"
      blurb="WP-S5 — WebSocket changefeed (lis realtime on :54323 when stack-full)."
    />
  );
}
