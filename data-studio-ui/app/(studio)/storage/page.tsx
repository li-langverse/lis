import { ComingSoon } from "@/components/coming-soon";

export default function StoragePage() {
  return <ComingSoon title="Storage" blurb="WP-S5 — object storage buckets (not configured in registry-min)." />;
}
