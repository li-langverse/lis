# Built-in leak detector enum IDs (no user regex).

def leak_pattern_openai_sk() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0

def leak_pattern_jwt_bearer() -> int
  requires true
  ensures result == 1
  decreases 0
=
  return 1

def leak_pattern_pem_private() -> int
  requires true
  ensures result == 2
  decreases 0
=
  return 2
