[package]
name = "li-schema"
version = "0.0.0-infra"
license = "GPL-3.0-or-later"
description = "Infrastructure scaffold — implementation pending (Li language stabilizing)."

[package.metadata]
status = "planned"
