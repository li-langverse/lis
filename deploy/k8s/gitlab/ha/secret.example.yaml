# Copy to secret.yaml, fill passwords, apply once. Never commit secret.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: gitlab-postgresql-secret
  namespace: gitlab
  labels:
    app.kubernetes.io/part-of: gitlab-ha
type: Opaque
stringData:
  postgresql-password: "CHANGE_ME"
